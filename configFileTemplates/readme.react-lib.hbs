# A project template for a React component library (React + TypeScript + Vite)

An opinionated setup for a React TypeScript component library project.

## Configuration

Remember to configure "main", "module", "types", "files" and "exports" properties in `package.json` according to the needs of your project.

{{> common}}

